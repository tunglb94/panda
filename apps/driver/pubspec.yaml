name: driver
description: FAIRRIDE Driver application.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  google_maps_flutter: ^2.10.0
  geolocator: ^13.0.0
  http: ^1.2.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
