org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Works around a Kotlin incremental-compiler crash ("this and base files have
# different roots") that happens when the project and the pub-cache/Flutter
# SDK live on different Windows drive letters (relativeTo() can't compute a
# relative path across drives). Build-machine-specific, not a source change.
kotlin.incremental=false
